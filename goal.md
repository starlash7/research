# UNIT TX Research Images Goal

## 한 문장 목표

이 저장소를 clone한 사람이 HTML이나 CSS를 다시 디자인하지 않고, 예제 JSON의 자료와 데이터만 교체해 UNIT TX의 일관된 Substack 커버와 본문 리서치 이미지를 만들 수 있게 한다.

## 사용 방식

1. 저장소를 clone한다.
2. `examples/*.json` 중 목적에 맞는 템플릿을 복제한다.
3. category, 제목, 설명, 출처, 날짜, 차트 데이터와 같은 자료와 데이터만 교체한다.
4. `.venv/bin/python render.py <input.json>` 또는 `.venv/bin/python render.py --all`을 실행한다.
5. `out/`의 PNG를 Substack에 사용한다.

반복 제작자는 템플릿 HTML과 공통 CSS를 직접 수정하지 않는다. 새로운 시각 규칙이 필요할 때만 규칙 파일과 템플릿을 함께 변경하고, 예제 전체를 다시 렌더해 일관성을 확인한다.

## 고정 출력 계약

- 메인 커버 `cover-*`: 1440×756px
- 본문 와이드 `figure-framework`: 1440×810px
- 본문 데이터 `figure-data`: 1440×1200px
- 모든 템플릿의 공식 UNIT TX 로고는 오른쪽 아래에 한 번만 둔다.
- 모든 템플릿의 날짜는 왼쪽 아래에 고정한다.
- 하단 푸터에는 날짜와 UNIT TX 락업만 두고 가운데 문구는 넣지 않는다.
- 두 커버는 왼쪽 위에 필수 `category`를 한 번만 표시한다.
- 템플릿이 장식 문구나 기본 대체 문장을 자동으로 넣지 않는다.
- 글꼴은 SUIT Variable, 굵기는 본문 500, 라벨 600, 제목과 브랜드 800으로 고정한다.
- UNIT TX 워드마크 글자는 밝은 배경 `#0C1B33`, 어두운 배경 `#FFFFFF`를 사용한다.
- 모든 날짜 글자는 `#000000`으로 고정한다. 어두운 배경에서는 흰색 바탕 위에 표시한다.
- 정보 카드와 이미지 프레임의 모서리 반경은 28px로 고정한다.
- 밝은 계열은 흰색 배경과 UNIT TX Blue `#0064FF`를 사용한다.
- 어두운 커버는 차콜, 스모크, 딥블루를 겹친 저채도 그라데이션을 기본으로 사용하며, 실제 이미지 입력이 있을 때만 이미지를 표시한다.
- 핵심 카피와 하단 메타데이터는 6% 안전 여백 안에 둔다.
- 수치 이미지에는 출처, 날짜, 단위와 범례를 포함한다.

## 품질 기준

일관성은 로고 위치, 날짜 위치, 캔버스 크기, 글꼴, 색상 토큰, 여백과 정보 계층으로 판단한다. 실제 PNG를 렌더한 뒤 원본 크기에서 잘림과 겹침을 확인하고, `sips`로 픽셀 크기를 검증한다.

구현 변경은 `python -m unittest discover -s tests -v`를 통과해야 한다. 생성된 HTML, PNG와 manifest는 `out/`에만 두고 커밋하지 않는다. 커밋 대상은 템플릿, 입력 JSON, 로컬 자산, 규칙과 테스트다.

## 범위 밖

이 저장소는 리서치 내용을 수집하거나 사실 여부를 자동 판정하지 않는다. 작성자는 실제 출처와 기준일을 확인하고 JSON에 넣는다. 이 저장소의 역할은 검증된 자료와 데이터를 동일한 UNIT TX 이미지 시스템으로 출력하는 것이다.
