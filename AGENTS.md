# UNIT TX Research Image Rules

이 저장소는 UNIT TX가 Substack 리서치에 사용하는 이미지 템플릿과 렌더러를 관리한다. clone 후 자료와 데이터만 교체해 일관된 결과를 만드는 것이 [goal.md](goal.md)의 목표다. 모든 변경은 아래 규칙과 [`docs/IMAGE_SYSTEM.md`](docs/IMAGE_SYSTEM.md)를 따른다.

## 고정 규격

- 메인 커버 `cover-*`: 1440×756px
- 본문 와이드 `figure-framework`: 1440×810px
- 본문 데이터 5종 `figure-data`, `figure-ranking`, `figure-composition`, `figure-metrics`, `figure-comparison`: 1440×1200px
- 템플릿 안에서 캔버스 크기를 임의로 바꾸지 않는다.
- 핵심 텍스트와 로고는 사방 6% 안전 여백 안쪽에 둔다.
- 모든 템플릿에서 공식 UNIT TX 로고는 오른쪽 아래에 한 번만 둔다.
- 모든 템플릿에서 날짜는 왼쪽 아래에 고정한다.
- 하단 푸터는 날짜와 UNIT TX 락업만 사용한다. 가운데 문구나 주제 라벨을 추가하지 않는다.
- 날짜 글자는 밝은 배경에서 검정 `#000000`, 어두운 배경에서 흰색 `#FFFFFF`으로 표시한다. 날짜에는 배경 박스나 테두리를 넣지 않는다.
- 두 커버는 왼쪽 위에 JSON의 `category`를 한 번만 표시한다.

## 브랜드

- `assets/unit-tx-logo.png`가 유일한 공식 심볼이다.
- 로고를 다시 그리거나 비율, 간격, 형태를 바꾸지 않는다.
- 밝은 배경에는 검정, 어두운 배경에는 흰색으로만 표시한다.
- UNIT TX 워드마크 글자는 밝은 배경에서 `#0C1B33`, 어두운 배경에서 `#FFFFFF`로 통일한다.
- 기본 글꼴은 저장소에 포함한 SUIT Variable 하나만 사용한다.
- 글자 굵기는 본문 500, 라벨과 메타데이터 600, 제목과 UNIT TX 워드마크 800만 사용한다.
- 정보 카드와 이미지 프레임의 모서리 반경은 28px로 통일한다.
- 공식 키컬러는 UNIT TX Blue `#0064FF`로 확정했다 (2026.09.11). 커버, 데이터 이미지, 프레임워크의 강조색은 이 블루로 통일한다. Toss Blue를 참고하되 토스 로고나 기타 브랜드 자산은 사용하지 않는다. 다른 블루 계열은 데이터 시리즈를 구분할 때만 쓴다.
- 공식 제작 JSON은 `accent`와 `palette_preview`를 생략해 기본 블루를 사용한다. `examples/previews/palettes/`의 다른 색상은 이전 비교 시안 보관용이며 공식 제작에 사용하지 않는다.
- 키컬러 통일은 흑백 UNIT TX 심볼, 워드마크, 날짜의 고정 색상이나 BTC 등 외부 프로젝트 원본 이미지의 색상을 바꾸는 뜻이 아니다.
- 어두운 계열은 차콜, 스모크, 딥블루를 겹친 저채도 그라데이션을 사용한다. 단색 검정, 그리드, 임의 그래프, 생성 심볼, 장식용 3D 오브젝트를 넣지 않는다.
- `cover-object`는 `hero_image`가 입력된 경우에만 실제 이미지를 표시한다. 이미지가 없으면 빈 공간을 유지한다.

## 카피와 데이터

- 커버 제목은 최대 두 줄이며 강제 줄바꿈은 한 번만 허용한다. 한 줄은 한글 15자 또는 영문 30자 폭 이내로 쓴다.
- 커버 `category`는 필수이며 영문 24자 폭 이내의 구체적인 분류명으로 쓴다.
- 장식용 문구보다 구체적인 주장과 대상이 드러나는 제목을 쓴다.
- `USE CASES`, `SYSTEM NOTE`, `KEY READ`처럼 내용을 추가하지 않는 장식 문구나 기본 대체 문장을 템플릿에서 자동 삽입하지 않는다.
- 수치가 들어간 이미지는 `source`와 `date`를 반드시 표시한다.
- 단위, 범례, 기준 시점 없이 차트를 만들지 않는다.
- 예시 수치는 반드시 `Example data · replace before publishing`으로 표시한다.
- 본문 데이터 5종은 흰색 배경과 공통 `data-base.html`의 제목, 메타데이터, 출처, 푸터 위치를 공유한다. 커버 스타일은 변경하지 않는다.
- 추세는 `figure-data`, 순위는 `figure-ranking`, 100% 구성비는 `figure-composition`, 핵심 지표 4개는 `figure-metrics`, 항목별 표는 `figure-comparison`을 쓴다.
- 데이터 제목은 최대 두 줄, 한 줄당 영문 44자 폭 이내로 입력한다. `period`는 분석 기간 또는 집계 기준을 추가할 때만 사용한다.
- 차트에 KPI나 설명 문구를 자동으로 추가하지 않는다. `takeaways`와 지표의 `change`는 입력된 경우에만 표시한다.
- 구성비는 입력 비율을 그대로 그리며 합계가 100%가 아니면 오류로 처리한다. 실제 데이터처럼 보이게 임의로 정규화하지 않는다.
- 색상만으로 상승, 하락, 시리즈 차이를 전달하지 않는다. 텍스트나 기호를 함께 쓴다.
- 이미지 안의 가시적 카피에는 em dash와 en dash를 사용하지 않는다. 범위와 구분에는 일반 하이픈을 쓴다.

## 자산과 출력

- 모든 이미지와 폰트는 로컬 자산으로 저장한다. 렌더 중 원격 URL을 요청하지 않는다.
- 반복 제작 시 HTML을 직접 수정하지 말고 `examples/*.json`과 같은 입력 JSON을 복제해 편집한다.
- slug는 영문 소문자, 숫자, 하이픈만 사용한다.
- 생성물은 `out/`에 두며 템플릿, JSON 입력, 테스트만 커밋한다.
- 실행 명령은 `.venv/bin/python render.py <input.json>` 또는 `.venv/bin/python render.py --all`이다.

## 완료 기준

- `python -m unittest discover -s tests -v`가 통과해야 한다.
- 실제 PNG를 렌더하고 `sips`로 픽셀 크기를 확인한다.
- 원본 크기로 열어 잘림, 겹침, 의도하지 않은 줄바꿈, 로고 사각 배경을 확인한다.
- 데이터 카드에서는 출처, 날짜, 단위, 범례가 모바일 축소 상태에서도 식별 가능해야 한다.
- 하단 고정 푸터의 날짜가 왼쪽, 로고가 오른쪽에 있는지 확인한다.
